# Do not edit this file.  To update the yml files, update the
# template file and run generate.py.

name: scipy.sparse, scipy build

on: workflow_dispatch

jobs:

  xslow-sparse:
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.14']
        os: [ubuntu-latest]

    runs-on: ${{ matrix.os }}

    env:
      SCIPY_XSLOW: 1

    steps:
      - uses: actions/checkout@v6
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
      - name: Update python pip, wheel, setuptools
        run: |
          python -m pip install --upgrade pip wheel setuptools
      - name: Install libopenblas-dev
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt install libopenblas-dev
      - name: Clone the scipy repo
        run: |
          git clone --shallow-submodules --recurse-submodules https://github.com/scipy/scipy.git
      - name: Install dev Python dependencies
        run: |
          cd scipy
          pwd
          python -m pip install --group dev
      - name: Test scipy.sparse with spin
        run: |
          cd scipy
          pwd
          spin test -m full -j 2 -s sparse --durations 10
