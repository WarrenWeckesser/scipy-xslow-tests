# scipy-xslow-tests
Run the SciPy XSLOW tests in github actions.

The actions are triggered by 'workflow_dispatch', meaning they are run by
going to the Actions tab on github, selecting one of the actions on the
left, and then clicking on the "Run workflow" button on the right.
